package yeet;

import java.util.Random;


public class RanGen {

    static public double fuck = 0.0;
    public int namehere[] = new int[10]; // the reason why you have to use the "new" token is because arrays are a actually their own kind of,,,,, object. the same way that an object is an object,; also uhhhharrays cant change indexes and stuff u can lokk at the documentation :) ik a decent part of this

    public RanGen() {
        //write code here
        Random asd = new Random();
        int n = asd.nextInt(100) + 1;
    }

    //push plos
    //wait where do i put the way to put values in the arra
    // make another method; like below
    void what() {
    }


    /*constructor syntax:
     *
     * [public/private] [static/non static (static means that you call the method like this: Classname.method() in stead of through the object. i dont really know how to use it for optimizatiuon; but you dig.] [i forot the other adjectives you can use to describe methods] [method name] ([parameters, delimited,m by, comma){
     *
     * function here.
     *
     * }
     * */

}
